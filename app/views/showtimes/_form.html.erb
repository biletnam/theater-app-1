<%= form_for @showtime do |f| %>
	Film: <%= select "showtime", "movie_id", Movie.all.collect { |movie| [movie.title, movie.id] } %><br>
	Auditorium: <%= select "showtime", "auditorium_id", Auditorium.all.collect { |auditorium| [auditorium.name, auditorium.id] } %><br>
	Date: <%= date_select("showtime", "date" ) %><br>
	Time: <%= time_select("showtime", "time", ampm: true ) %><br>
	<%= f.submit 'Create' %>
<% end %> 